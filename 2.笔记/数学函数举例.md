#3-已上传 
# 数学函数举例

```mysql
# round 四舍五入
select round(-1.65);
select round(1.567,2);

# ceil 向上取整
select ceil(1.00);

# floor 向下取整
select floor(1.00);

# truncate 去尾
select truncate (1.65,1);

# mod 取余
select mod(10,-3);
select 10 % 3;
```